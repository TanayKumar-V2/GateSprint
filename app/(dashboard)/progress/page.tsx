export default function ProgressPlaceholderPage() {
  return (
    <section aria-labelledby="progress-heading" className="max-w-2xl">
      <h1 id="progress-heading" className="text-2xl font-semibold tracking-tight">
        Progress
      </h1>
      <p className="mt-2 text-sm leading-6 text-muted-foreground">
        Accuracy aggregates and weak-topic guidance land in Phase 5. Empty
        states will avoid misleading percentages.
      </p>
    </section>
  );
}
